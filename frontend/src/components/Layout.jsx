import { useState } from 'react';
import { NavLink, Outlet, useNavigate } from 'react-router-dom';
import { BarChart3, Building2, Check, ChevronLeft, CircleUserRound, Heart, KeyRound, LayoutDashboard, LogOut, Menu, Star, Store, Users, X } from 'lucide-react';
import { useAuth } from '../context/AuthContext';

const menus = { ADMIN: [{ to: '/admin/dashboard', label: 'Overview', icon: LayoutDashboard }, { to: '/admin/users', label: 'Users', icon: Users }, { to: '/admin/stores', label: 'Stores', icon: Building2 }, { to: '/admin/ratings', label: 'Ratings', icon: Star }], USER: [{ to: '/user/dashboard', label: 'Dashboard', icon: LayoutDashboard }, { to: '/user/stores', label: 'Discover stores', icon: Store }, { to: '/user/ratings', label: 'My ratings', icon: Star }, { to: '/user/favorites', label: 'Favorites', icon: Heart }], OWNER: [{ to: '/owner/dashboard', label: 'Performance', icon: BarChart3 }, { to: '/owner/store', label: 'My store', icon: Building2 }, { to: '/owner/ratings', label: 'Customer ratings', icon: Star }] };
export default function Layout() { const { user, logout } = useAuth(); const [open, setOpen] = useState(false); const navigate = useNavigate(); const items = menus[user.role] ?? []; function signOut() { logout(); navigate('/login'); }
 return <div className="dashboard-shell"><aside className={`sidebar ${open ? 'open' : ''}`}><div className="brand"><span className="brand-mark"><Check size={18} strokeWidth={3} /></span><span>Trust<span>Mark</span></span><button className="icon-button sidebar-close" onClick={() => setOpen(false)} aria-label="Close menu"><X size={18} /></button></div><div className="workspace-label">WORKSPACE</div><nav>{items.map(({ to, label, icon: Icon }) => <NavLink onClick={() => setOpen(false)} className={({ isActive }) => isActive ? 'nav-link active' : 'nav-link'} to={to} key={to}><Icon size={18} />{label}</NavLink>)}</nav><div className="sidebar-bottom"><NavLink className="nav-link" to="/profile"><CircleUserRound size={18} />Profile</NavLink><NavLink className="nav-link" to="/change-password"><KeyRound size={18} />Change password</NavLink><button className="nav-link logout" onClick={signOut}><LogOut size={18} />Sign out</button></div></aside><div className="main-column"><header className="topbar"><button className="icon-button menu-button" onClick={() => setOpen(true)} aria-label="Open menu"><Menu size={20} /></button><div className="crumb"><span>Workspace</span><ChevronLeft size={14} /><strong>{user.role.toLowerCase()} portal</strong></div><div className="user-chip"><span className="avatar">{user.name?.charAt(0)}</span><span>{user.name}</span><span className="role-badge">{user.role}</span></div></header><main className="page-content"><Outlet /></main></div></div>; }
