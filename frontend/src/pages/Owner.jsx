import { useEffect, useState } from 'react';
import { Mail, Star } from 'lucide-react';
import api, { messageFrom } from '../services/api';
import RatingStars from '../components/RatingStars';
export default function OwnerRatings() { const [ratings, setRatings] = useState([]); const [error, setError] = useState(''); useEffect(() => { api.get('/owner/ratings').then((res) => setRatings(res.data.data)).catch((err) => setError(messageFrom(err))); }, []); return <><div className="page-heading"><div><p className="eyebrow">CUSTOMER FEEDBACK</p><h1>Users who rated your store</h1><p className="muted">Read every signal and keep improving the experience.</p></div></div>{error && <div className="alert error">{error}</div>}<section className="panel table-wrap"><table><thead><tr><th>Customer</th><th>Rating</th><th>Comment</th><th>Updated</th><th>Contact</th></tr></thead><tbody>{ratings.map((item) => <tr key={item.id}><td><div className="table-person"><span className="avatar soft">{item.user.name.charAt(0)}</span><strong>{item.user.name}</strong></div></td><td><div className="rating-inline"><RatingStars value={item.rating} />{item.rating}/5</div></td><td>{item.comment || 'No written review'}</td><td>{new Date(item.updatedAt).toLocaleDateString()}</td><td><span className="contact"><Mail size={14} />{item.user.email}</span></td></tr>)}</tbody></table>{!ratings.length && <div className="empty-inline"><Star size={22} /><p>No ratings yet</p></div>}</section></>; }
